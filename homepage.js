var playFriend = document.querySelector(".btn-playfriend");
var play = document.querySelector(".btn-play");
var username = prompt("Choose a username!");
console.log(username);
class player {
  constructor(username) {
    this.username = username;
  }
  // search (username){
  //     const add = prompt("Search for an username")
  // if (username === add)

  // }
}
